# Secrets never enter the repo. State (store + raw archive) is deliberately
# TRACKED: workflow runs commit their own results, making provenance durable
# and versioned. Only logs and scratch stay out.
config/secrets.yaml
data/pull-logs/
__pycache__/
*.pyc
.DS_Store
partner_site/.vercel/
partner_site/.env.local
.vercel
.env*
# Google service account key: never commit
config/google-service-account.json
